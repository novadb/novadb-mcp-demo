{
  "name": "novadb-mcp",
  "version": "1.4.0",
  "description": "MCP server for NovaDB CMS and Index APIs",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "novadb-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run tests/",
    "test:watch": "vitest tests/",
    "build:ext": "node scripts/build-claude-extension.mjs",
    "build:plugin": "node scripts/build-claude-plugin.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.0.0"
  }
}
